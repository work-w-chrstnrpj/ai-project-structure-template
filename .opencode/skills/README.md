# OpenCode Skill Adapters

This folder contains generated OpenCode skill copies.

Canonical skill instructions live in `.agents/skills/`. Run `scripts/sync-ai-adapters.ps1 -Target opencode` after changing a skill.
