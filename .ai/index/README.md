# Generated Index Placeholder

This folder is reserved for generated indexes such as file maps, symbols, routes, dependencies, or embeddings.

Do not commit generated index files unless the project explicitly decides they are stable and useful in version control.

Document the generation command here after the project chooses one.
