# Codex Adapters

Codex custom agent adapters are generated from canonical role files.

- Agents: `.codex/agents/*.toml`
- Skills: `.agents/skills/<skill>/SKILL.md`

Edit `.agents/roles/` or `.agents/skills/`, then run `scripts/generate-ai-adapters.ps1`.
